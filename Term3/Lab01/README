Лабораторная №1
Структуры данных

Задачи:

	перемещающий конструктор
	перемещающий оператор присваивания
	конструирование из списка инициализации (std::initializer_list).
	insert
	emplace
	итератор
	параметризовать класс
	winapi-интерфейс

1.	Многочлен – KPolynomial
               Методы: 
                    конструкторы, деструктор;
                    операции: [], =, ==, !=, +, +=, –, –=, *, *=, <<, >>
                            операция [] предоставляет доступ к i-му члену полинома, а класс члена полинома 
                            предоставляет методы Coeff() и Degree()
                    функции:
						Count() – количество членов полинома
						Degree() – степень полинома;
						Clear()   - удалить все члены полинома (т.е. сделать полином пустым (нулевым) ),
						Swap()  - обменять значения (т.е. обменять полиномы) .
